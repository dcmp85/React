import React from 'react';

const Button = (props) => {
    return (
        <div className="col-2">
            <button type="button" className="btn btn-primary">=</button>
        </div>
    );
}

export default Button;